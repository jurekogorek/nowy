<h1>Listing dupas</h1>

<table>
  <tr>
    <th>Nazwa</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @dupas.each do |dupa| %>
  <tr>
    <td><%= dupa.nazwa %></td>
    <td><%= link_to 'Show', dupa %></td>
    <td><%= link_to 'Edit', edit_dupa_path(dupa) %></td>
    <td><%= link_to 'Destroy', dupa, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Dupa', new_dupa_path %>
