<%= form_for(@dupa) do |f| %>
  <% if @dupa.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dupa.errors.count, "error") %> prohibited this dupa from being saved:</h2>

      <ul>
      <% @dupa.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nazwa %><br />
    <%= f.text_field :nazwa %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
